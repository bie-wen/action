name: test

on: [push, pull_request_target]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@master

      - name: checkout
        run: |
          echo ${{ github.ref }}
          echo ${{ github.sha }}
          echo ${GITHUB_SHA}
          echo ${{ toJson(github) }}
